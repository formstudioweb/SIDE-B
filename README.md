# SIDE B — GitHub Pages

Завантажте **весь вміст цієї папки** в корінь репозиторію. Потім відкрийте `Settings → Pages`, оберіть `Deploy from a branch`, гілку `main` і папку `/root`.

Не змінюйте структуру папки `images` — інакше зображення не завантажаться.
